<%= form_for @user, :url => user_path, :html => { :multipart => true } do |f| %>
  <p>
    <%= f.file_field :avatar %>
  </p>
  <button type="submit" class="btn btn-primary btn-sm actions text-center">Submit</button>
<% end %>
